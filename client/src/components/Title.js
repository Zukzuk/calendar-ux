import React from "react";

const Title = () => {
    return (
        <div style={{ display: "flex", alignItems: "center", gap: "10px" }}>
            <h1>Gedeelde kalender</h1>
        </div>
    );
};

export default Title;
